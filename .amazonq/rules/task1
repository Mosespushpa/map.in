https://mosespushpa.github.io/map.in/
this is where I hosted the website.
go through it.

Here are few task that has to be done.

When navbar logo and title is click the page should reset to home page.

When clicked on States tag all the state should be highlighted with state border.

Union Territories tag when clicked it should highlighted and at that position their name should be displayed.

for now in Rivers tag, just display the river name in future we will update the river logic.

When clicked on Ghats tag, that particular area should be highlighted with their name displayed.

Same for Forts tag too pin-point the location with name displayed.

Languages make it like each state with their originated language.

for Dynasties too display the name of them and also highlight the area of the dynasties covered.

Pin-point the exact location of the Historical events and display their name in the map.

IMPORTANT THING

CORRECTLY CONFIGURE THE TIME LINE WITH MAP AND TAGS.

WHEN A CURRENT ACTION/EVENT HAPPENING IN THE SITE THE OLD ONE SHOULD BE GONE. SO THAT IT WILL NOT INTERFIOR THE CURRENT EVENT.

IN INFORMATION PANNEL, GIVE THE EXACT SAME SIZE OF THE STATE STATS DIV FOR IMAGES. MAKE THE STATE STATS AND IMAGE AS SLIDER IN THEIR PARTICULAR AREA. This Image part should added to everything for tags, timeline, map etc.

before you start implementing, clarify any doubt if you have any.

Let me first explore the current codebase to understand what's already built before asking clarifying questions.

1 directory listed
map
2 files read
index.html
script.js
3 files read
tags.js
timeline.js
sidepanel.js
3 files read
style.css
tags.css
timeline.css
2 files read
rivers.js
states.json
Now I have a thorough understanding of the codebase. Before implementing, here are my clarifying questions:

Clarifications needed:

States tag — Should clicking "States" highlight ALL states simultaneously with colored borders (like a choropleth), or just enable the hover/click behavior that's already there?

Union Territories — The UTs (Delhi, Puducherry, Ladakh, J&K, Dadra & NH & DD, Lakshadweep, Andaman & Nicobar, Chandigarh) are already in the SVG as paths. Should they be highlighted in a distinct color with their names shown as SVG text labels on the map?

Ghats — Western Ghats and Eastern Ghats span multiple states. Should the highlight be a colored overlay on the states they cover, or a separate SVG polygon/region? And for Varanasi/Rishikesh/Haridwar ghats — those are point locations, so should they be pin markers?

Forts — Currently they're pin markers (circles). Should they show the fort name as a text label next to the pin, or just on hover/click?

Languages — Should each state be filled with a unique color per language group (like a language map), with the language name shown as text on the state? Or just color-coded without text?

Dynasties — Dynasties covered multiple overlapping regions historically. Do you have specific boundary data in mind, or should I use approximate state-level coverage (e.g., Mughal = North India states highlighted)?

Historical Events — These are point locations (e.g., Battle of Plassey, Partition). Should they show as pin markers with name labels directly on the map?

Timeline + Map sync — When a year is selected on the timeline, should it: (a) just fade out states that didn't exist yet (current behavior), or (b) also automatically switch to a relevant category/overlay?

Info Panel image slider — Where should the images come from? Placeholder images (via a URL like picsum/unsplash), or do you have actual images? And should the stats grid and image slider be side-by-side or stacked?

"Reset to home" — Should clicking the logo/title clear all active overlays, reset the timeline to 2025, reset the tag to "States", and close the side panel?

States tag — Should clicking "States" highlight ALL states simultaneously with colored borders (like a choropleth) - Yes, also display the state names. And when hovered/clicked the same behavior.

2.Union Territories — The UTs (Delhi, Puducherry, Ladakh, J&K, Dadra & NH & DD, Lakshadweep, Andaman & Nicobar, Chandigarh) are already in the SVG as paths. Should they be highlighted in a distinct color with their names shown as SVG text labels on the map? - Yes.

Ghats — Western Ghats and Eastern Ghats span multiple states. a separate SVG polygon/region. And for Varanasi/Rishikesh/Haridwar ghats — those are point locations, so should they be pin markers? -Yes.

4.Forts — Currently they're pin markers (circles). Should they show the fort name as a text label next to the pin? - Yes.

5.Languages — Should each state be filled with a unique color per language group (like a language map), with the language name shown as text on the state? - Yes, it will look good.

6.Dynasties — Dynasties covered multiple overlapping regions historically. should I use approximate state-level coverage (e.g., Mughal = North India states highlighted)? for now approximate level.

7.Historical Events — These are point locations (e.g., Battle of Plassey, Partition). Should they show as pin markers with name labels directly on the map? - Yes

Timeline + Map sync — When a year is selected on the timeline, should it: (a) just fade out states that didn't exist yet (current behavior), or (b) also automatically switch to a relevant category/overlay? use automatic switch.

Info Panel image slider — Where should the images come from? Placeholder images (via a URL like picsum/unsplash), or do you have actual images? And should the stats grid and image slider be side-by-side or stacked? use upload images later, and stats grid and image slider should stacked.

"Reset to home" — Should clicking the logo/title clear all active overlays, reset the timeline to 2025, reset the tag to "States", and close the side panel? Yaa.

remember for every event there must be information and images in the information panel.

still any clarification needed.

A few more quick clarifications before I start:

State name labels on map — The SVG viewBox is 432x488 which is quite small. Some states like Goa, Sikkim, Chandigarh are tiny. Should I show labels only for states above a certain size, or show all labels (even if they overlap on small states)?

Dynasties data — I'll map dynasties to approximate states. Should I include these major dynasties?

Mughal Empire (North + Central India)

Maratha Empire (Maharashtra + surrounding)

Vijayanagara Empire (Karnataka + AP + TN)

Chola Dynasty (Tamil Nadu + parts of AP)

Maurya Empire (Bihar + UP + MP + surrounding)

Rajput Kingdoms (Rajasthan + parts of MP)

Nizam of Hyderabad (Telangana + parts of AP + Karnataka)

Or do you want different/more dynasties?

Historical Events pin locations — Should I include events like:

Battle of Plassey (West Bengal, 1757)

First War of Independence (Delhi/Meerut, 1857)

Partition of India (Punjab/Bengal, 1947)

Dandi March (Gujarat, 1930)

Jallianwala Bagh (Punjab, 1919)

Battle of Panipat (Haryana, 1526/1556/1761)

Confirm this list or add/remove?

Image upload later — So for now I'll add a placeholder image slot with a "Upload Image" style placeholder box in the panel, and the slider UI will be ready for when images are added. Correct?

Timeline auto-switch — When year 1947 is clicked, it auto-switches to "Historical Events" category? Or switches to "States" and shows which states existed then? What's the logic for which category to auto-switch to per year?

State name labels on map. no problem, just show the labels.

Dynasties data — I'll map dynasties to approximate states. Should I include these major dynasties? Yes include all of them, show the dynasty name in the map with its capital and (time period of the dynasty), with this configure the timeline too. when the particular timeline is selected the dynasty should be highlighted.

3.Historical Events pin locations, yes include everything, if any thing miss i will add later.

4.Image upload later — So for now I'll add a placeholder image slot with a "Upload Image" style placeholder box in the panel, and the slider UI will be ready for when images are added. Correct? yaa, keep the logic for it.

5.Timeline auto-switch — When year 1947 is clicked, it auto-switches to "Historical Events" category.
still any

Two final quick ones:

Dynasty timeline years — The current timeline has fixed years (1947, 1950, 1956...). Dynasties span very different eras (Maurya: 322 BCE, Mughal: 1526 AD, etc.). Should I:

Add a separate dynasty timeline that appears only when "Dynasties" tag is active (with BCE/CE years like 322BCE, 1206, 1336, 1526, 1674, 1724...)

Or keep the existing 1947–2025 timeline and just show dynasty info as an overlay regardless of year?

Overlapping dynasties on map — Mughal and Maratha empires overlap heavily (both cover Maharashtra, MP, UP etc.). When both are shown, should I:

Show them with semi-transparent different colors so overlaps are visible

Or show one dynasty at a time (click a dynasty name to highlight just that one)

Dynasty timeline years — The current timeline has fixed years (1947, 1950, 1956...). Dynasties span very different eras (Maurya: 322 BCE, Mughal: 1526 AD, etc.). Should I:

Add a separate dynasty timeline that appears only when "Dynasties" tag is active (with BCE/CE years like 322BCE, 1206, 1336, 1526, 1674, 1724...)

2.Overlapping dynasties on map — Mughal and Maratha empires overlap heavily (both cover Maharashtra, MP, UP etc.). When both are shown, should I:

for now, Show them with semi-transparent different colors so overlaps are visible

done.

Perfect. Now I have everything I need. Let me plan this out before coding:

Files to modify: script.js, index.html, style.css, components/tags.js, components/timeline.js

Files to create: components/overlays.js, components/overlays.css

Let me start implementing all features systematically.

1 file read
search.js
Good. Now I have the full picture. Let me implement everything. This is a large implementation — I'll do it in organized batches.

continue from where you left


Context
Let me start implementing everything now. I'll create/modify files systematically.

overlays.js
Creating the main overlays engine that handles all tag categories: states, UTs, rivers, ghats, forts, languages, dynasties, historical events.



@Pin Context